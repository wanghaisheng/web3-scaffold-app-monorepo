<!-- input: copied upstream BMAD usage guidance and the repo-local Quick/BMM execution model -->
<!-- output: a compatibility note for readers opening the copied complete usage guide -->
<!-- pos: reference-layer complete usage compatibility guide -->
# Complete Usage Compatibility Guide

This copied guide remains available for broad upstream BMAD context.

For the shared terminology and path translation table, use `.codex/reference/LEGACY-MAPPING.md`.

It is not the active day-to-day operating manual for this repository.

## Current Local Model

The local execution surface is intentionally compressed:

- `Quick` for bounded delivery
- `BMM` for larger or unclear work

Everything else is handled by repo-local support docs under `.codex/core/`, `.codex/maps/`, and `.codex/templates/`.

## Start Here Instead

- `AGENTS.md`
- `.codex/README.md`
- `.codex/workflows/router.md`

## Legacy Mapping

- `Quick Dev` -> local `Quick`
- full BMAD planning flows -> local `BMM`
- `_bmad-output/changes/...` -> legacy path
- `openspec/changes/{change-name}/` -> preferred local path

Use this guide only for upstream terminology or historical cross-reference.
