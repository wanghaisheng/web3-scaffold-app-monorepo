<!-- input: copied legacy unified router wrapper and the repo-local workflow model -->
<!-- output: a compatibility note for the copied legacy unified workflow -->
<!-- pos: reference-layer legacy unified workflow compatibility guide -->
# Legacy Unified Workflow Compatibility Guide

This copied file documents an older unified router model and is retained for historical context only.

The active local router is:

- `.codex/workflows/router.md`

## Local Routing Model

The router now selects between only two user-facing modes:

- `.codex/workflows/quick.md`
- `.codex/workflows/bmm.md`

## Legacy Mapping

- `Quick Dev` -> local `Quick`
- broader planning, research, or design flows -> local `BMM`
- older `@workflow-*` routing trees are copied reference material only

Use this file only as background, not as the local workflow authority.
