<!-- input: historical alias usage and the repo-local workflow model -->
<!-- output: a compatibility note for the copied @archive alias -->
<!-- pos: reference-layer archive alias compatibility guide -->
# `@archive` Compatibility Guide

`@archive` is a legacy alias page kept for compatibility context.

The active local equivalent is:

- `.codex/workflows/archive.md`

## Local Mapping

- older `Quick Dev` archive wording maps to the current closeout and archive flow
- new work should archive through the local workflow docs, not through copied wrapper text

Use this alias page only as historical reference.
