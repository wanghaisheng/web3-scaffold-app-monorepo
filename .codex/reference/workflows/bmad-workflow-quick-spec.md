<!-- input: copied quick-spec workflow wrapper and the repo-local execution model -->
<!-- output: a compatibility note for the copied quick-spec workflow wrapper -->
<!-- pos: reference-layer quick-spec workflow compatibility guide -->
# Quick Spec Workflow Compatibility Guide

This copied wrapper is kept for upstream workflow context.

The active local equivalent is:

- `.codex/workflows/bmm.md` for spec-led work
- `.codex/workflows/quick.md` for bounded work that still needs a single local milestone definition

## Local Interpretation

- older `quick-spec` language maps into the local `Quick` plus `BMM` split
- new non-trivial change records should use `openspec/changes/{change-name}/`
- `_bmad-output/changes/...` in copied material is legacy only

Use repo-local workflow docs for current planning behavior.
