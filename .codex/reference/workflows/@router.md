<!-- input: copied @router alias semantics and the repo-local routing model -->
<!-- output: a compatibility note for the copied @router alias -->
<!-- pos: reference-layer @router compatibility alias -->
# `@router` Compatibility Note

This copied alias remains available for historical prompt compatibility.

In this repository, the active router is:

- `.codex/workflows/router.md`

## Local Interpretation

- `@router` maps conceptually to the local router, but the current repo-local rules live in `.codex/workflows/router.md`
- `Quick Dev` in older material maps to local `Quick`
- broader BMAD planning routes map to local `BMM`
- current mode decisions use repo-local sizing, milestone, and harness rules rather than copied OpenSpec enforcement text

Use the repo-local router for current behavior.
