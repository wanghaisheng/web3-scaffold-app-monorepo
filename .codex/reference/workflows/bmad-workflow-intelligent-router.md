<!-- input: copied intelligent-router workflow wrapper and the repo-local routing model -->
<!-- output: a compatibility note for the copied intelligent-router workflow wrapper -->
<!-- pos: reference-layer intelligent-router workflow compatibility guide -->
# Intelligent Router Workflow Compatibility Guide

This copied wrapper is kept for upstream routing context.

The active local router is:

- `.codex/workflows/router.md`

## Local Interpretation

- the local router chooses only between `Quick` and `BMM`
- support workflows remain internal support workflows, not primary user-facing modes
- current routing decisions rely on local sizing, milestone, and harness docs rather than copied wrapper config
