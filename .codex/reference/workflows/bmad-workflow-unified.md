<!-- input: copied legacy unified workflow wrapper and the repo-local routing model -->
<!-- output: a compatibility note for the copied unified workflow wrapper -->
<!-- pos: reference-layer unified workflow compatibility guide -->
# Unified Workflow Compatibility Guide

This copied wrapper is legacy material.

Its local meaning now routes through:

- `.codex/workflows/router.md`

## Local Interpretation

- the old unified-router idea has been absorbed into the local router
- the local router now selects only between `Quick` and `BMM`
- any larger copied BMAD mode surface should be treated as upstream background, not the active local contract
