<!-- input: copied workflow-init wrapper and the repo-local routing model -->
<!-- output: a compatibility note for the copied workflow-init workflow -->
<!-- pos: reference-layer workflow-init compatibility guide -->
# Workflow Init Compatibility Guide

This copied wrapper describes an older BMM initialization flow and is retained for upstream background only.

The active local start points are:

- `AGENTS.md`
- `.codex/README.md`
- `.codex/workflows/router.md`

## Local Mapping

- older `workflow-init` behavior maps into routing through local `BMM` when discovery or planning is needed
- copied initialization wrappers are not the local source of truth

Use this file only when local routing docs are not enough.
