<!-- input: historical alias usage and the repo-local BMM workflow model -->
<!-- output: a compatibility note for the copied @brief alias -->
<!-- pos: reference-layer brief alias compatibility guide -->
# `@brief` Compatibility Guide

`@brief` is a legacy alias page retained for compatibility.

Brief-style product discovery now belongs inside:

- `.codex/workflows/bmm.md`

Use this alias page only as a historical mapping note.
